spring:
  datasource:
    url: jdbc:mysql://localhost:3306/restaurant_db?createDatabaseIfNotExist=true&serverTimezone=America/Sao_Paulo
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: false

    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: info

springdoc:
  swagger-ui:
    path: /swagger

server:
  port: 8080

jwt:
  secret: ${API_SECRET:uma-chave-super-secreta-com-pelo-menos-32-caracteres-123456}
  expiration: 36000